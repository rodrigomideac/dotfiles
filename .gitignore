!.*
*.swp
.aider*
.claude/worktrees/
.config/systemd/user/default.target.wants/
# atuin's official-installer receipt is rewritten on every self-update; the
# config dir is a symlink into this repo, so ignore it to avoid churn.
.config/atuin/atuin-receipt.json
